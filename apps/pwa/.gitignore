# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# PWA dev service worker output
dev-dist/

# Environment files
# Committed: the per-environment build config (.env.development/.test/.production).
# VITE_* values are public — they are inlined into the client bundle, so nothing
# secret may live in them. Ignored: personal machine overrides.
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
