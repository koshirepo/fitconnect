# Documentation: Local environment template.
# - Provides the minimum environment variables needed to boot the API locally, including a development JWT secret and a fallback database URL.
# - Copy these values into `.env` for local development and replace the defaults before using any shared or persistent environment.
DATABASE_URL="file:./prisma/local.db"
JWT_SECRET="dev-local-jwt-secret-change-me"
